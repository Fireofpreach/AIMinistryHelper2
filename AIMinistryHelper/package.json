{
  "name": "aiministryhelper2",
  "version": "1.0.0",
  "description": "AIMinistryHelper2 project",
  "main": "dist/index.js",
  "scripts": {
    "start": "NODE_ENV=production node dist/index.js",
    "build": "esbuild server/index.ts --platform=node --bundle --format=esm --outdir=dist && npm run build:client",
    "build:client": "npx vite build",
    "dev": "npx vite"
  },
  "dependencies": {
    // Add your runtime dependencies here, e.g.:
    // "express": "^4.17.1",
    // "react": "^18.2.0"
  },
  "devDependencies": {
    // Add your development dependencies here, e.g.:
    // "esbuild": "^0.17.0",
    // "vite": "^4.0.0",
    // "typescript": "^5.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
